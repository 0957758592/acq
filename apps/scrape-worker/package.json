{
  "name": "@acq/scrape-worker-app",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "./src/worker.js",
  "scripts": {
    "start": "node ./src/worker.js",
    "test": "cross-env NODE_OPTIONS=--experimental-vm-modules jest -c jest.config.js --passWithNoTests",
    "test:live": "cross-env NODE_OPTIONS=--experimental-vm-modules jest -c jest.live.config.js --runInBand",
    "lint": "eslint .",
    "build": "node -e \"console.log('scrape-worker-app: no build step')\""
  },
  "dependencies": {
    "@acq/scraping": "*",
    "@acq/engine-domain": "*",
    "@acq/engine-infra": "*",
    "@acq/core": "*",
    "@acq/logger": "0.1.0"
  },
  "devDependencies": { "cross-env": "^7.0.3", "eslint": "^9.17.0", "jest": "^29.7.0" }
}
